# Base URL for the Spring Boot API. The dev server also proxies /api to
# http://localhost:5000 (see vite.config.js), so this is only needed for
# production builds served separately from the backend.
VITE_API_URL=http://localhost:5000/api
